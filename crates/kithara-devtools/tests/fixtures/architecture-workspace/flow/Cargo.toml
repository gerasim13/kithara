[package]
name = "flow"
version.workspace = true
edition.workspace = true
license.workspace = true

[package.metadata.cargo-machete]
# Fixture data for the architecture tests: these edges exist so the
# analyser has a graph to read, not so the code calls anything.
ignored = ["conditional", "support"]

[dependencies]
conditional = { path = "../conditional", optional = true }
support = { path = "../support" }
