[package]
name = "kithara-drm"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "AES-128 DRM decryption for streaming audio, extensible keys."
keywords = ["drm", "aes", "audio", "hls", "streaming"]
categories = ["multimedia::audio", "cryptography"]

[package.metadata.cargo-machete]
ignored = ["kithara-workspace-hack"]

[features]
default = ["client-reqwest", "tls-rustls"]
# HTTP backend forwarding for standalone builds; kithara-drm reaches
# kithara-net only through kithara-assets. Workspace-level
# default-features = false keeps the default off for consumers.
client-reqwest = ["kithara-assets/client-reqwest"]
client-wreq = ["kithara-assets/client-wreq"]
tls-native = ["kithara-assets/tls-native"]
tls-rustls = ["kithara-assets/tls-rustls"]

[dependencies]
kithara-assets = { workspace = true }
kithara-platform = { workspace = true }

aes = { workspace = true }
bon = { workspace = true }
bytes = { workspace = true }
cbc = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
url = { workspace = true }

[target.'cfg(all(not(target_arch = "wasm32"), not(target_os = "android")))'.dependencies]
kithara-workspace-hack = { version = "0.0.1-alpha4", path = "../kithara-workspace-hack" }

[dev-dependencies]
kithara-drm = { path = ".", default-features = false }
kithara-test-utils = { path = "../kithara-test-utils", features = ["mock", "probe", "hang"] }
wasm-bindgen-test = { workspace = true }

[lints]
workspace = true
