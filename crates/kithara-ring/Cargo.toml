[package]
name = "kithara-ring"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Lock-free SPSC ring over caller-owned contiguous storage."
keywords = ["ringbuffer", "spsc", "lock-free", "audio"]
categories = ["concurrency", "data-structures"]

[dependencies]
ringbuf = { workspace = true }

[target.'cfg(all(not(target_arch = "wasm32"), not(target_os = "android")))'.dependencies]
kithara-workspace-hack = { version = "0.0.1-alpha4", path = "../kithara-workspace-hack" }

[dev-dependencies]
kithara-test-utils = { workspace = true }

[lints]
workspace = true
