# On-disk fixture cache (L2). ON BY DEFAULT: when KITHARA_FIXTURE_CACHE is unset
# (the default profile, `just test`, `cargo test`, `just rtsan`, IDE), the test
# code falls back to a build-fingerprinted temp dir (see
# tests/src/fixture_cache.rs::default_cache_dir), so encode/mux output is reused
# across the per-test processes in a run. Without L2, nextest's process-per-test
# model re-encodes every AAC fixture from scratch — under CPU oversubscription a
# single test's ffmpeg encode can exhaust its whole time budget and time out.
#
# The opt-in `cache` profile below additionally runs a setup script that wipes
# and recreates a shared cache dir and exports KITHARA_FIXTURE_CACHE, giving an
# ephemeral, explicitly-scoped per-run cache instead of the persistent default.
experimental = ["setup-scripts"]

[scripts.setup.fixture-cache]
command = 'cargo run -q -p kithara-integration-tests --bin fixture-cache-setup'

[profile.default]
default-filter = '''
not (binary(suite_stress) + binary(suite_heavy))
+ test(=abr_contract::probe_commit_sequence::auto_commit_flips_active_variant)
+ test(=kithara_audio::alloc_free_hotpath::resampler_active_steady_state_alloc_free)
+ test(=kithara_decode::fixture_integration::test_packaged_hls_aac_and_flac_roundtrip_decode_descending_saw_flac)
+ test(=kithara_decode::gapless_encoding_parity::gapless_encoding_variants_yield_matching_decoder_metadata)
+ test(=kithara_decode::gapless_parity::generated_encoded_signal_visible_frames_match_requested_signal_frames_flac)
+ test(=kithara_decode::hls_abr_variant_switch::test_abr_variant_switch_no_byte_glitches)
+ test(=kithara_decode::stress_seek_random::stress_random_seek_read_synthetic_wav)
+ test(=kithara_decode::stress_timeline::stress_seeks_preserve_timeline_integrity)
+ test(=kithara_file::live_stress_real_mp3::live_stress_real_mp3_seek_read_cache_ephemeral)
+ test(=kithara_hls::abr_auto_switch::abr_auto_switch_during_playback)
+ test(=kithara_hls::abr_mode_switch::seek_backwards_after_manual_switch_to_uncached_variant_does_not_hang_hw_same_codec_aac_low_to_high)
+ test(=kithara_hls::abr_switch_playback::abr_switch_real_assets_does_not_hang)
+ test(=kithara_hls::abr_switch_playback::seek_after_eof_mmap_produces_samples_drm)
+ test(=kithara_hls::abr_switch_playback::stream_continues_after_seek_drm_abr_auto_hw)
+ test(=kithara_hls::abr_switch_playback::stream_continues_after_seek_drm_abr_auto_sw)
+ test(=kithara_hls::abr_switch_playback::stream_continues_after_seek_drm_manual_v0_hw)
+ test(=kithara_hls::abr_switch_playback::stream_continues_after_seek_drm_manual_v0_sw)
+ test(=kithara_hls::deferred_abr_debug::debug_sequential_read)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_drm_disk_auto)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_drm_disk_v0)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_drm_disk_v3)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_drm_eph_auto)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_drm_eph_v0)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_drm_eph_v3)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_hls_disk_auto)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_hls_disk_v0)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_hls_disk_v3)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_hls_eph_auto)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_hls_eph_v0)
+ test(=kithara_hls::drm_stream_integrity::drm_stream_byte_integrity_hls_eph_v3)
+ test(=kithara_hls::idle_behavior::idle_prefetch_is_capped)
+ test(=kithara_hls::live_stress_real_stream::live_ephemeral_revisit_sequence_regression_drm_hw)
+ test(=kithara_hls::live_stress_real_stream::live_ephemeral_revisit_sequence_regression_drm_sw)
+ test(=kithara_hls::live_stress_real_stream::live_ephemeral_small_cache_playback_drm)
+ test(=kithara_hls::live_stress_real_stream::live_ephemeral_small_cache_playback_hls)
+ test(=kithara_hls::live_stress_real_stream::live_ephemeral_small_cache_seek_stress_drm_hw)
+ test(=kithara_hls::live_stress_real_stream::live_ephemeral_small_cache_seek_stress_drm_sw)
+ test(=kithara_hls::live_stress_real_stream::live_real_drm_playback_smoke)
+ test(=kithara_hls::live_stress_real_stream::live_real_stream_fixed_seek_window_regression_drm)
+ test(=kithara_hls::live_stress_real_stream::live_real_stream_random_seek_prefix_regression_drm)
+ test(=kithara_hls::live_stress_real_stream::live_real_stream_seek_resume_native_drm)
+ test(=kithara_hls::live_stress_real_stream::live_stress_real_stream_seek_read_cache_drm_ephemeral)
+ test(=kithara_hls::live_stress_real_stream::live_stress_real_stream_seek_read_cache_drm_mmap)
+ test(=kithara_hls::red_flaky_small_cache_hot_refetch::red_flaky_small_cache_hot_refetch_behind_reader)
+ test(=kithara_hls::red_leak_native_drm_seek_resume::red_leak_native_drm_seek_resume_thread_budget)
+ test(=kithara_hls::startup_no_eager_size_probe_storm::startup_issues_no_eager_size_probe_storm_flac_fmp4)
+ test(=kithara_hls::stress_chunk_integrity::stress_chunk_integrity_ephemeral)
+ test(=kithara_hls::stress_seek_abr::seek_sequence_from_log_real_stream_drm)
+ test(=kithara_hls::stress_seek_abr::seek_sequence_from_log_real_stream_hls)
+ test(=kithara_hls::stress_seek_abr::stress_seek_during_abr_switch_real_decoder_drm)
+ test(=kithara_hls::stress_seek_abr_audio::stress_seek_abr_audio_flac_fmp4)
+ test(=kithara_hls::stress_seek_audio::stress_seek_audio_hls_wav_apple_ephemeral)
+ test(=kithara_hls::stress_seek_lifecycle::stress_seek_lifecycle_with_zero_reset_ephemeral)
+ test(=kithara_hls::stress_seek_random::stress_random_seek_read_hls_encrypted_medium)
+ test(=kithara_hls::stress_seek_random::stress_random_seek_read_hls_encrypted_small)
+ test(=kithara_hls::stress_seek_random::stress_random_seek_read_hls_small)
+ test(=kithara_play::flac_realtime_player_continuity::flac_realtime_player_continuity_sustained_flac_symphonia)
+ test(=kithara_play::hls_seek_middle_stress_long::hls_seek_middle_repeated_seeks_long_stress_apple)
+ test(=multi_instance::concurrent_file::concurrent_file_instances_n2)
+ test(=multi_instance::concurrent_hls::concurrent_hls_instances_n2_manual)
+ test(=multi_instance::concurrent_mixed::concurrent_mixed_instances_two_file_two_hls)
+ test(=multi_instance::failure_resilience::healthy_instances_survive_cancelled_peers_h2_c2)
+ test(=phase_continuity::common::tests::anchor_detects_sample_drop_of_two)
+ test(=phase_continuity::file::phase_continuity_file_sentinel_mp3_apple_eph_e2e)
+ test(=phase_continuity::hls::phase_continuity_hls_aac_lc_symphonia_eph_manual_multi)
+ test(=phase_continuity::hls::phase_continuity_hls_diag_paced_aac_he_v2_symphonia_pace1ms)
+ test(=phase_continuity::hls::phase_continuity_hls_diag_paced_aac_lc_symphonia_pace1ms)
+ test(=phase_continuity::hls::phase_continuity_hls_known_warmup_drift_cross_codec_switch_apple_drm)
+ test(=phase_continuity::hls::phase_continuity_hls_known_warmup_drift_cross_codec_switch_symphonia_drm)
'''
fail-fast = false
test-threads = "num-cpus"
# Wall-clock BACKSTOP only. The real per-test bound is the macro `timeout(...)`
# on each test — its abort watchdog kills a hung test at its own budget+3s, so
# this backstop only matters for tests with NO per-test timeout. It must sit
# above the slowest *actual* real runtime (NOT above the largest budget): the
# full-suite worst case under contention is ~57s (a flash(false) real-socket
# downswitch test). 120s = ~2x that — well clear of every legit run, while a
# real wedge fails in 120s instead of 300s. A flash deadlock is normally caught
# far sooner by the virtual hang-watchdog (~1s).
slow-timeout = { period = "120s", terminate-after = 1 }
# Default 100ms is too tight when 12 cores run 1800+ tests concurrently;
# munmap of a 46MB test binary + stdout flush can exceed it even for
# sync tests that spawn no threads. A real leak (live worker thread
# blocked on channel) would still exceed 2s and get caught.
leak-timeout = "2s"

[profile.ci]
fail-fast = false
test-threads = "num-cpus"
leak-timeout = "2s"

# Fast-only profile: heavy tests are excluded via -E filter in justfile.
[profile.fast]
fail-fast = false
test-threads = "num-cpus"
leak-timeout = "2s"

# Stress/full profile: run the complete matrix, including stress/heavy variants.
# No thread limit — a real audio player shares the machine with many
# other processes. Tests must pass under CPU contention.
[profile.stress]
default-filter = "all()"
fail-fast = false
test-threads = "num-cpus"
# Wall-clock backstop above the slowest actual runtime (~57s under contention),
# NOT above the per-test budget — the macro `timeout(...)` abort watchdog is the
# real per-test bound. A wedge fails in 120s, not 300s.
slow-timeout = { period = "120s", terminate-after = 1 }
leak-timeout = "2s"

# Opt-in L2 fixture-cache profile: same settings as default, plus the
# setup script that prepares the cache dir and exports KITHARA_FIXTURE_CACHE.
# Use via `cargo nextest run --profile cache` / `just test-cached`.
[profile.cache]
fail-fast = false
test-threads = "num-cpus"
slow-timeout = { period = "120s", terminate-after = 1 }
leak-timeout = "2s"

[[profile.cache.scripts]]
filter = 'package(kithara-integration-tests)'
setup = ['fixture-cache']

# Perf-measurement profile: keep the full set so performance measurement does
# not inherit the default canary split.
[profile.perf]
default-filter = "all()"

[profile.perf.junit]
path = "junit.xml"
