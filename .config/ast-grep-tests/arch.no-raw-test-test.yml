id: arch.no-raw-test
valid:
  - |
    #[kithara::test]
    fn uses_workspace_test_harness() {}
  - |
    #[kithara::test(native, tokio)]
    async fn uses_configured_workspace_test_harness() {}
invalid:
  - |
    #[test]
    fn uses_raw_rust_harness() {}
  - |
    #[cfg(feature = "optional-analysis")]
    #[test]
    fn stacked_attributes_still_expose_raw_test() {}
